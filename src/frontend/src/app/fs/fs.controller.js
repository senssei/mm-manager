import Controller from '../../core/controller';

export const name = 'FSController';
export class FSController extends Controller {
  /*@ngInject*/
  constructor($scope) {
    super($scope);
  }

  $setup() {
    
  }
}